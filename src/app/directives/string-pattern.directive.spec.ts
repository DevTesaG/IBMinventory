import { StringPatternDirective } from './string-pattern.directive';

describe('StringPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new StringPatternDirective();
    expect(directive).toBeTruthy();
  });
});
